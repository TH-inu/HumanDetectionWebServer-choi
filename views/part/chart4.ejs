<script>
    var rowData = JSON.parse('<%- JSON.stringify(csi_data) %>');
    var data = dataProcessing(rowData);

    let graph4 = Highcharts.chart('section4Graph', {

        title: {
            text: 'Show Real Time CSI Data Graph4'
        },
        yAxis: {
            title: {
                text: 'amplitude'
            }
        },
        xAxis: {
            accessibility: {
                rangeDescription: 'Range: 1 to 52'
            }
        },

        legend: {
            layout: 'vertical',
            align: 'right',
            verticalAlign: 'middle'
        },

        plotOptions: {
            series: {
                label: {
                    connectorAllowed: false
                },
                pointStart: 1
            }
        },

        series: data,   // 그래프 데이터

        responsive: {
            rules: [{
                condition: {
                    maxWidth: 500
                },
                chartOptions: {
                    legend: {
                        layout: 'horizontal',
                        align: 'center',
                        verticalAlign: 'bottom'
                    }
                }
            }]
        }
    });
</script>